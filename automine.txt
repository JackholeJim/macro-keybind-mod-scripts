if(mining);
    log(&cAutoMine Stopped);
    unset(mining);
    stop();
    keyup(forward);
else;
    if(!mining);
        log(&aStarting AutoMine);
        set(mining);
        do;
            do(15);
                key(attack);
            loop;
            keydown(forward);
            wait(100ms);
            keyup(forward);
        loop;
    endif;
endif;
